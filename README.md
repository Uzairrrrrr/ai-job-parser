# Job Description Extractor Agent

This script reads a CSV of job listings and uses OpenAI's GPT model to extract structured data like job summary, key responsibilities, and required skills.  
It then saves the results into a new processed CSV file for easy analysis.
